;(define fact
    ;(lambda (n)
   ;     (if (= n 0)
  ;          1
 ;           (* n (fact (- n 1))))))

;(define id (lambda (x) x))
;(define foo
 ;   (lambda (x y)
  ;      (id x)))

;(foo 3 4)

;(fact 2)

(if '2/3 #f '0notasymbol)

